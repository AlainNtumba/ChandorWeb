@using ChandorAdmin.Services
@inject AppNavigation AppNavigation
@code {
    private bool _redirected;

    protected override void OnAfterRender(bool firstRender)
    {
        if (!firstRender || _redirected)
            return;

        _redirected = true;
        AppNavigation.NavigateToLogin(replace: true);
    }
}
